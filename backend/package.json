{
  "name": "billion-portfolio-whatsapp-api",
  "version": "1.0.0",
  "description": "Tiny backend that securely sends a WhatsApp Cloud API notification when the Billion Portfolio contact form is submitted.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0"
  }
}
